[project]
name = "chunker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Guanling Wang", email = "g.wang@framence.com" }
]
requires-python = ">=3.14"
dependencies = [
    "markdown-it-py>=4.2.0",
]

[project.scripts]
chunker = "chunker:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
